/*
describe('ProjectListController', function() {
	
	beforeEach(module('projectApp'));
	
	it('should create a `projects` model with 1 project', inject(function($controller) {
		
		var scope = {};
		var ctrl = $controller('ProjectListController', {$scope: scope});
		
		expect(scope.name).toBe('world');
		expect(scope.projects.length).toBe(1);
	}));
	
});
*/